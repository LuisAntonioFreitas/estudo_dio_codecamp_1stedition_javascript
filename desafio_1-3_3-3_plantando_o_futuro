// Desafio: Plantando o Futuro
// 3 / 3 - Plantando o Futuro

//Desafios JavaScript na DIO têm funções "gets" e "print" acessíveis globalmente:
//- "gets" : lê UMA linha com dado(s) de entrada (inputs) do usuário;
//- "print": imprime um texto de saída (output), pulando linha.

let id = parseInt(gets());

//TODO: Print no console se a semente deve ser plantada ou se o buraco deve
//avançar ou retroceder X metros

let distancia = 10;
let espaco = id;

if (espaco === distancia) {
  print("Plantar!");
} else {
  if (espaco > distancia) {
    print(`O buraco deve retroceder ${espaco-distancia} metros`);
  } else {
    print(`O buraco deve avançar ${distancia-espaco} metros`);
  }
}
